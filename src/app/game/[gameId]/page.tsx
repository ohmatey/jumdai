import MemoryGame from '@/app/features/MemoryGame'

const GamePage = () => {
  return (
    <MemoryGame
      // settings={settings}
      // onEndGame={() => setStarted(false)}
    />
  )
}

export default GamePage